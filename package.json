{
  "name": "interview-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "interview",
    "api"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace interview\"",
    "start": "concurrently \"npm start --workspace backend\" \"npm run preview --workspace interview\"",
"postinstall": "shx chmod +x ./node_modules/.bin/concurrently && shx chmod +x ./node_modules/.bin/vite",
    "build": "concurrently \"npm run build --workspace interview\" \"npm run build --workspace api || echo 'No build for api'\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "shx": "^0.4.0"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0"
  }
}
