{
  "name": "interview-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "interview",
    "api"
  ],
"scripts": {
  "build": "npx --ignore-existing concurrently \"npm run build --workspace interview\" \"npm run build --workspace api || echo 'No build for api'\""
},
  "devDependencies": {
    "concurrently": "^7.6.0",
    "shx": "^0.4.0"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0"
  }
}
