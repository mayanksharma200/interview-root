import { build } from "vite";

build().catch((err) => {
  console.error(err);
  process.exit(1);
});
import { build } from 'vite';

build().catch((err) => {
  console.error(err);
  process.exit(1);
});